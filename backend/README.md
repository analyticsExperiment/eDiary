# Backend Server

This is a minimal Node.js server using the built-in `http` module. It exposes a simple REST API for diary entries and analytics.

## Running

```
node server.js
```

The server listens on port `3000` by default.
